name: Bonjour
on:
  - push
jobs:
  dire_bonjour:
    runs-on: ubuntu-22.04
    steps:
      - run: echo "Bonjour 👋"
  cow_personalisation:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install cowsay
      - run: cat README.md | cowsay
  cow_prefab:
    runs-on: ubuntu-22.04
    container:
      image: ghcr.io/cicd-lectures/gitpod:latest # !! latest !!
      options: --user=root
    steps:
      - uses: actions/checkout@v4
      - run: cat README.md | cowsay 